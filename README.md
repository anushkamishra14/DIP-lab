# DIP-lab
The file DIPfundamentals is the file that contains the basic codes to convert an image to grayscale, red channel, upload an iamge and convert it into black and white image.
the second file Histeq contains the method to equalise an image and create the histogram to represet the distribution of the pixels based on the intensity of the pixels and their number, it contains both the mathematical method and the direct keyword to do the above mentioned.
